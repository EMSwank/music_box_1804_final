<% @genres.each do |genre| %>
  <%= genre.name %>
    <% end %>

      Create new genre:
      <%= form_for @genre do |f| %>
        <%= f.label :name %>
          <%= f.text_field :name %>
            <%= f.submit %>
              <% end %>