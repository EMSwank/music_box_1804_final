<h1>
  <%= @song.title %>
</h1>
<h3>
  <%= @song.length%>
</h3>
Song rating:
<%= @song.rating%>

  <h1>Genres for this Song:</h1>
  <% @song.genres.each do |genre| %>
    <%= genre.name %>

      <% end %>